1. Driver[] drivers = new Driver[5];
2.
for(int i; i <= drivers.length;  i++) {
	driver[i] = new Driver();
}
3.
for(int i; i <= drivers.length;  i++) {
	driver[i] = new Driver();
}

4.
Hashmap<Strings, int> nameMap
